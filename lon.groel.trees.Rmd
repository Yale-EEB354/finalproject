---
title: "Lon and GroEL Trees"
output: html_document
---

```{r setup, echo = FALSE, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(ape)
```



```{r fig.width=5, fig.height=5}
lon.phy = read.tree("lon.aligned.fasta.treefile")
plot(lon.phy)

```


```{r}
lon.phy$Nnode
```

```{r fig.width=5, fig.height=5}
groel.phy = read.tree("groel.aligned.fasta.treefile")

plotTree(groel.phy)
edgelabels()
```

```{r}
groel.phy$Nnode
```

```{r}
## check if tree & rt.all are equal
all.equal(groel.phy, lon.phy,rt.all)

```

```{r}
#node.depth(groel.phy, method = 1) 
#node.depth.edgelength(groel.phy) 
#node.height(groel.phy, clado.style = FALSE)

```

```{r}
str(groel.phy)
str(lon.phy)
```
```{r}
lon.phy$edge
lon.phy$edge.length

groel.phy$edge
groel.phy$edge.length
```
```{r}
timetree.phylo(groel.phy, node.time=c("expert","mean","median"))
```






